class Reducer{
	const counter = (state = {}, action) => {
	  switch (action.type) {
	    case 'EDIT':
	      return ;
	    case 'DECREMENT':
	      return ;
	    default:
	      return state;
	  }
}

export default Reducer;